-- ============================================================
-- Banco de Dados: Concessionária Auto Fácil (MySQL)
-- ============================================================

-- ------------------------------------------------------------
-- 1. CRIAÇÃO DAS TABELAS (DDL)
-- ------------------------------------------------------------

CREATE TABLE veiculos (
    id_veiculo INT AUTO_INCREMENT PRIMARY KEY,
    modelo VARCHAR(50) NOT NULL,
    marca VARCHAR(50) NOT NULL,
    ano INT NOT NULL,
    preco DECIMAL(10,2) NOT NULL
);

CREATE TABLE clientes (
    id_cliente INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    telefone VARCHAR(20) NOT NULL
);

CREATE TABLE vendedores (
    id_vendedor INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    comissao DECIMAL(4,2) NOT NULL
);

CREATE TABLE vendas (
    id_venda INT AUTO_INCREMENT PRIMARY KEY,
    id_veiculo INT NOT NULL,
    id_cliente INT NOT NULL,
    id_vendedor INT NOT NULL,
    data_venda DATE NOT NULL,
    valor_final DECIMAL(10,2) NOT NULL,
    FOREIGN KEY (id_veiculo) REFERENCES veiculos(id_veiculo),
    FOREIGN KEY (id_cliente) REFERENCES clientes(id_cliente),
    FOREIGN KEY (id_vendedor) REFERENCES vendedores(id_vendedor)
);

-- ------------------------------------------------------------
-- 2. INSERÇÃO DOS DADOS INICIAIS (DML)
-- ------------------------------------------------------------

INSERT INTO veiculos (id_veiculo, modelo, marca, ano, preco) VALUES
(1, 'Corolla', 'Toyota', 2022, 95000.00),
(2, 'Civic', 'Honda', 2023, 105000.00),
(3, 'Onix', 'Chevrolet', 2021, 75000.00);

INSERT INTO clientes (id_cliente, nome, telefone) VALUES
(1, 'Carlos Silva', '61988887777'),
(2, 'Ana Souza', '61999996666'),
(3, 'João Pereira', '61888885555');

INSERT INTO vendedores (id_vendedor, nome, comissao) VALUES
(1, 'Fernanda Lima', 0.05),
(2, 'Roberto Carlos', 0.04);

INSERT INTO vendas (id_venda, id_veiculo, id_cliente, id_vendedor, data_venda, valor_final) VALUES
(1, 1, 1, 1, '2025-05-20', 93000.00),
(2, 2, 2, 2, '2025-05-22', 102000.00),
(3, 3, 3, 1, '2025-05-23', 74000.00);

-- ------------------------------------------------------------
-- 3. ALTERAÇÕES E ATUALIZAÇÕES
-- ------------------------------------------------------------

-- Adição da coluna cor na tabela veiculos
ALTER TABLE veiculos ADD cor VARCHAR(30);

-- Atualização das cores dos veículos inseridos
UPDATE veiculos SET cor = 'Preto' WHERE id_veiculo = 1;
UPDATE veiculos SET cor = 'Branco' WHERE id_veiculo = 2;
UPDATE veiculos SET cor = 'Prata' WHERE id_veiculo = 3;

-- Atualização do telefone do cliente João Pereira
UPDATE clientes SET telefone = '61777774444' WHERE id_cliente = 3;

-- Exclusão da venda com id_venda = 3
DELETE FROM vendas WHERE id_venda = 3;

-- ------------------------------------------------------------
-- 4. CONSULTA RELACIONAL COM INNER JOIN
-- ------------------------------------------------------------

SELECT
    vendas.data_venda,
    vendas.valor_final,
    vendedores.nome,
    clientes.nome,
    veiculos.modelo,
    veiculos.marca
FROM vendas
INNER JOIN vendedores ON vendas.id_vendedor = vendedores.id_vendedor
INNER JOIN clientes ON vendas.id_cliente = clientes.id_cliente
INNER JOIN veiculos ON vendas.id_veiculo = veiculos.id_veiculo;
